[build-system]
requires = ["hatchling>=1.27"]
build-backend = "hatchling.build"

[project]
name = "yngmin-python-formatter"
version = "0.3.0"
description = "Python formatter and linter for yngmin's Python Style Guide"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "black>=26.3.1",
    "libcst>=1.8.0",
]

[project.optional-dependencies]
dev = [
    "pyright>=1.1.393",
    "pytest>=8.0",
]

[project.scripts]
yngfmt = "yngfmt.cli:main"
ynglint = "yngfmt.lint_cli:main"

[tool.hatch.build.targets.wheel]
packages = ["src/yngfmt"]

[tool.pyright]
include = ["src", "tests"]
pythonVersion = "3.11"
typeCheckingMode = "standard"
reportMissingTypeStubs = "none"

[tool.pytest.ini_options]
pythonpath = ["src"]
testpaths = ["tests"]

[tool.yngfmt.imports]
first-party = ["yngfmt"]
language-segment = "language"
config-segment = "config"

[tool.yngfmt.result-object]
class-names = ["Result"]
typed-dict-names = ["ResultDict"]
required-fields = ["error", "code", "message", "data"]
marker-fields = ["error", "code"]
aliases = ["success", "msg", "payload"]
